def mergeTwoLists(a, b):
    dummy = cur = ListNode(0)
    while a and b:
        if a.val < b.val: cur.next, a = a, a.next
        else: cur.next, b = b, b.next
        cur = cur.next
    cur.next = a or b
    return dummy.next
